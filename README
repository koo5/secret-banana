editor that keeps each word as a separate item in an array, and annotates it with meaning (the blue text on each other line) 'from an autocompletion menu', thus programmer would be anotating (doing tokenization) by accepting or rejecting menu items, while the menu tries to stay out of the way of a smooth writing experience. on the second screenshot is visible an attempt at parsing the edited code and adding a new item to the menu.
